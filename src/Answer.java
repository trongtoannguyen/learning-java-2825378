class Answer {
    // Find the last character in the string (text)
    static char findLastCharacter(String text) {
        // Your code goes here.
        char lastCharacter = text.charAt(text.length() - 1);
        return lastCharacter;
    }
}